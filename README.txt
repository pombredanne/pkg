pkg is a tool for managing macOS packages.

Usage:

	pkg command [arguments]

The commands are:

	version     print pkg version
	install     install packages
	list        list packages
	info        get information from packages
	files       list files from packages
	which       display which package installed a specific file

Use "pkg help [command]" for more information about a command.
